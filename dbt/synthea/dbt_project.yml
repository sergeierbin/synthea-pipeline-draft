name: synthea
version: "1.0.0"
profile: synthea

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  synthea:
    staging:
      +materialized: view
    marts:
      +materialized: table
